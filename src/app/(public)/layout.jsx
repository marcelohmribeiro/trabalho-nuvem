/**
 * Public Layout
 *
 * Layout para rotas públicas (login, register)
 * Responsabilidade: Prover estrutura comum para páginas públicas
 */

export default function PublicLayout({ children }) {
  return children;
}
